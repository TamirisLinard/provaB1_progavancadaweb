// src/config/database.ts
import { Sequelize } from 'sequelize';

const sequelize = new Sequelize({
  dialect: 'sqlite',
  storage: 'database.sqlite', // ou use outro banco de dados como MySQL ou PostgreSQL
});

export default sequelize;
